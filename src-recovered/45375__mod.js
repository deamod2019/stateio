/**
 * Webpack Module #45375
 * (barrel / re‑export module)
 */
// (e/*module*/, t/*exports*/, n/*__require*/) =>
{
  ;("use strict")
  function i(e, t) {
    return null != e ? e : t()
  }
  n.d(t, { h: () => i })
}
