/**
 * Shared default options for status alerts.
 */
"use strict"

const defaultAlertOptions = {
  autoHide: true,
  autoHideTime: 3500,
  withIcon: true,
  withCloseIcon: true,
  removeAllBeforeShow: true,
}

module.exports = { defaultAlertOptions }
