/**
 * Restored boundary for the sprite-loader runtime used by generated SVG symbols.
 *
 * The implementation is still the original third-party runtime snapshot; keeping it
 * behind this named adapter removes direct numbered-module references from restored
 * UI source without changing sprite registration behavior.
 */
"use strict"

module.exports = require("../../src-cjs/95348__mod.js")
