/**
 * Restored boundary for the SVG path parser used by PathBounds.
 *
 * The parser implementation remains the original runtime snapshot for now; the
 * named boundary keeps core geometry source free of direct numbered imports.
 */
"use strict"

module.exports = require("../../src-cjs/66721__mod.js")
