/**
 * Restored source for Webpack Module #89559.
 *
 * Barrel export for the 2D preload action.
 */
"use strict"

const { PreloadAssetsAction } = require("./PreloadAssetsAction")

const exportsObject = { PreloadAssetsAction }

Object.defineProperty(exportsObject, "__esModule", { value: true })

module.exports = exportsObject
