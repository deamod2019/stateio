/**
 * Webpack Module #17673
 * (barrel / re‑export module)
 */
// (e/*module*/, t/*exports*/, n/*__require*/) =>
{
  ;("use strict")
  ;((t.decode = t.parse = n(62587) /* 62587__mod */), (t.encode = t.stringify = n(12361) /* 12361__mod */))
}
