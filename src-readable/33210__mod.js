/**
 * Webpack Module #33210
 * (barrel / re‑export module)
 * @esmodule
 */
// (e/*module*/, t/*exports*/) =>
{
  ;("use strict")
  ;(Object.defineProperty(t, "__esModule", { value: true }),
    (t.isTag = undefined),
    (t.isTag = function (e) {
      var t = typeof e
      return "string" === t || "number" === t
    }))
}
